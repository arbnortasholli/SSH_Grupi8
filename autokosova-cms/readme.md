# AutoKosova Admin

React + Vite admin panel for AutoKosova. The structure follows `autokosova-web`: routes, layouts, context, hooks, services, pages, components, config, utils, and shared types.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`
