services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: autokosova-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "AutoKosova_Str0ng!"
    ports:
      - "1433:1433"
    volumes:
      - autokosova-sql-data:/var/opt/mssql

volumes:
  autokosova-sql-data:
